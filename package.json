{
	"name": "protodb",
	"version": "0.0.1",
	"description": "ProtoDB",
	"main": "dist/client/index.js",
	"typings": "dist/client",
	"bin": "dist/main.js",
	"scripts": {
		"build-ts": "rm -rf dist && tsc && tsc -p client && cp pb/*.proto dist/pb && chmod a+x dist/main.js",
		"build-napi": "node-gyp rebuild && mkdir -p dist && mv build/Release/mmap.node dist/mmap-wrapper.node && rm -r build",
		"build": "npm run build-ts && npm run build-napi",
		"prepack": "npm run build-ts",
		"install": "npm run build-napi",
		"test": "npm run build && tsc -p test && nyc --reporter=lcov ava dist/test/main.js"
	},
	"files": [
		"binding.gyp",
		"dist/**/*.js",
		"dist/pb/*.proto",
		"dist/client/index.d.ts",
		"dist/pb/interface.d.ts",
		"mmap-wrapper/mmap.c"
	],
	"author": "Caleb Sander",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^12.0.4",
		"@types/yargs": "^13.0.0",
		"ava": "^1.4.1",
		"nyc": "^14.1.1",
		"typescript": "^3.4.2"
	},
	"dependencies": {
		"protobufjs": "^6.8.8",
		"yargs": "^13.2.4"
	}
}

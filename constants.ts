// The default TCP port to listen on
export const DEFAULT_PORT = 9000

// The default directory to store data files
export const DEFAULT_DATA_DIR = 'data'

// The number of random bytes to use for an iterator handle
export const ITER_BYTE_LENGTH = 16